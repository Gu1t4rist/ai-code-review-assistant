# AI Code Review Assistant

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)

AI-агент для автоматизации code review процессов в GitLab.

## Краткое описание проекта

### 🎯 Цель
Автоматизировать процесс code review в GitLab, снизить нагрузку на senior разработчиков и повысить качество кода за счет быстрого AI-анализа каждого Merge Request.

### 📋 Описание проблемы
В современной разработке ПО code review является критически важным, но времязатратным процессом:
- Senior разработчики тратят 20-30% времени на проверку чужого кода
- Ручной review часто пропускает типовые проблемы (SQL injection, N+1 queries, memory leaks)
- Отсутствие единых стандартов проверки между командами
- Задержки в процессе разработки из-за ожидания review

### ✅ Решение
AI Code Review Assistant - интеллектуальный ассистент, который:
- **Автоматически анализирует** каждый MR через GitLab webhooks
- **Обнаруживает проблемы** безопасности, производительности, качества кода
- **Оставляет комментарии** с точными номерами строк, объяснениями и готовыми решениями
- **Формирует резюме** с рекомендацией: approve / needs fixes / reject
- **Работает 24/7** - быстрая обратная связь без ожидания human reviewer

Технологии: FastAPI + GitLab API + Claude AI (Anthropic)

### 💼 Бизнес-эффект
- **Экономия времени**: Senior разработчики тратят на 40-60% меньше времени на типовые проверки
- **Повышение качества**: Автоматическое обнаружение 15+ категорий проблем (security, performance, bugs)
- **Ускорение разработки**: Среднее время review сокращается с 2-4 часов до 5 минут
- **Обучение команды**: Junior разработчики получают instant feedback с примерами best practices
- **Унификация стандартов**: Единые правила проверки для всех команд и проектов
- **Измеримые метрики**: ROI - экономия ~20-30 часов senior времени в неделю на команду из 10 человек

## Технологический стек

- **Python 3.10+**
- **GitLab API**: httpx (async HTTP client)
- **AI/LLM**: Anthropic Claude 3 Haiku
- **Web Framework**: FastAPI (async webhooks)
- **Logging**: structlog (structured JSON logging)

## Быстрый старт

### Локальное тестирование (Development)

```bash
# Установите зависимости
pip install -r requirements.txt

# Настройте .env файл
cp .env.example .env
nano .env

# Запустите webhook server
python -m src.ai_code_review.main webhook
```

**Для получения webhooks локально используйте ngrok:**

```bash
# Установите ngrok

sudo apt update && sudo apt install ngrok

# Запустите ngrok (в отдельном терминале)
ngrok http 8000

# Используйте HTTPS URL из ngrok для webhook
# Пример: https://abc123.ngrok.io/webhook
```

### Production deployment

См. **[DEPLOYMENT.md](DEPLOYMENT.md)** для production развертывания.

## Настройка GitLab Webhook

1. Settings → Webhooks
2. URL: 
   - **Локально**: `https://your-ngrok-url.ngrok.io/webhook`
   - **Production**: `https://code-review.company.com/webhook`
3. Events: `Merge request events`
4. Secret Token из `.env`

## Как работает

1. ✅ Получает webhook от GitLab при создании MR
2. ✅ Анализирует код с помощью Claude AI
3. ✅ Находит проблемы (security, performance, bugs)
4. ✅ Оставляет комментарии с решениями
5. ✅ Формирует резюме с рекомендацией

## Типы проверок

- **Безопасность**: SQL injection, XSS, hardcoded secrets, ...
- **Производительность**: N+1 queries, memory leaks, blocking operations, ...
- **Качество кода**: SOLID principles, design patterns, naming, ...
- **Архитектура**: Code structure, modularity, separation of concerns, ...
